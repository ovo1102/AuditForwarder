﻿# Test configuration
agent:
  id: "test-agent"
  data_dir: "C:\\AuditData"

log:
  level: debug
  file: "C:\\Logs\\auditforwarder.log"

collectors:
  process:
    enabled: true

manager:
  enabled: false

self_protect:
  enabled: false
